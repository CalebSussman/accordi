# Agent Prompts

This folder stores self-contained initialization prompts for agent dispatch. The master source is `governance/agent_work_orders.md`; prompt files here duplicate the current prompts for easier handoff.
